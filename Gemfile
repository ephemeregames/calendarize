source 'http://rubygems.org'

gemspec

gem 'jquery-rails'
gem 'calendarize', path: '../calendarize'
gem 'haml'

# Gems used only for assets and not required in production environments by default.
group :assets do
  gem 'sass-rails'
  gem 'coffee-rails'
end
